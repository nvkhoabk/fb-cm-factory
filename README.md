# FB-CM Factory

V2 automation factory for AI image, video, music, and composition workflows.

Backend API examples:

- `GET /health`
- `GET /workflows`
- `GET /workflow-runs`
- `GET /instance-pools`
- `GET /character-groups`
- `GET /prompt-templates`
- `GET /assets`

V1 reference repo: https://github.com/nvkhoabk/fb-cm-manager
